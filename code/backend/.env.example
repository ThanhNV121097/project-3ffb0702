# PostgreSQL connection string injected by runtime and docker compose.
DATABASE_URL=postgres://app:app_secret@postgres:5432/app?sslmode=disable

# HTTP listen port. PORT wins over APP_PORT.
PORT=8080

# Legacy fallback HTTP port for local runs when PORT is unset.
APP_PORT=8080
